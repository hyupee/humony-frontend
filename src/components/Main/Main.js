import React from 'react';
import Section from '../Section';
import Section1 from './SectionList';


const Main = () => {
  const sectionList = [
    { component: '' }
  ];

  return (
    <>
      <Section children={<p>as</p>} />
    </>
  );
};

export default Main;